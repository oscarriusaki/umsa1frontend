import { TextoCortoPipe } from './texto-corto.pipe';

describe('TextoCortoPipe', () => {
  it('create an instance', () => {
    const pipe = new TextoCortoPipe();
    expect(pipe).toBeTruthy();
  });
});
